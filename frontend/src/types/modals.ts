export type User = {
    name: string;
    email: string;
};

export type VideoType = {
    id: string;
    url: string;
    title: string;
    sharedBy: string;
    likes: number;
    dislikes: number;
    description: string;
    youtube_id: string;
};
